# Point Cloud Hidden Point Removal using Camera Trajectory

Implementation of a frame-wise Hidden Point Removal (HPR) pipeline for dynamic point clouds based on the viewport-dependent optimization idea presented in the ISM 2024 paper.

The project processes point cloud sequences together with a camera trajectory and removes points that are not visible from the user's viewpoint.

---

## Project Structure

```text
project/
│
├── dataset/
│   └── 8i/
│       ├── frame0001.ply
│       ├── frame0002.ply
│       └── ...
│
├── hpr_traj.py
├── hpr_static.py
├── render.py
│
├── campath_0.json #json file of camera trajectory
│
├── hpr_output/
│   └── longdress/
│
└── README.md
```

---

## Dependencies

Python 3.11

```bash
pip install numpy open3d pillow
```

Optional:

```bash
ffmpeg
```

for video generation.

---

## Method

For every frame:

```text
P_i -> HPR -> P'_i
```

The ISM24 implementation uses three viewpoints:

```text
C1 = user viewpoint

C2 = C1 + [deltaX, 0, 0]

C3 = C1 - [deltaX, 0, 0]
```

Visible points are computed from all viewpoints:

```text
Visible = HPR(P,C1) ∪ HPR(P,C2) ∪ HPR(P,C3)
```

The final point cloud is generated by taking the union of all visible points.

---

## Trajectory-based HPR

Input:

```text
frame1.ply  <-> camera pose 1
frame2.ply  <-> camera pose 2
...
frameN.ply  <-> camera pose N
```

Run:

```bash
python hpr_traj.py
```

Output:

```text
hpr_output/
└── longdress/
    ├── frame0001_hpr_cam0001.ply
    ├── frame0002_hpr_cam0002.ply
    └── ...
```

---

## Static HPR

A fixed camera position can also be used without trajectory information.

Run:

```bash
python hpr_static.py
```

---

## Render Comparison Video

Generate side-by-side viewport comparison of original and hpr-processed point cloud video.

Run:

```bash
python render.py
```

Output:

```text
demo_compare.mp4
```


## Parameters

### Multi-view offset

```python
DELTA_X = 500
```

### HPR radius factor

```python
HPR_RADIUS_FACTOR = 10000 
```

Higher values generally preserve more visible points.

